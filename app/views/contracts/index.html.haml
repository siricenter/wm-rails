.container-fluid
  %h1 Listing contracts

  %table.table.table-bordered
    %tr
      %th Name
      %th Email
      %th Home address 1
      %th Home address 2
      %th Home city
      %th Home state
      %th Home zip
      %th Parking type
      %th Preferences (If any)
      %th Number (If known)
      %th Date Created

    - @contracts.each do |contract|
      %tr
        %td= "#{contract.first_name} #{contract.last_name}"
        %td= contract.email
        %td= contract.home_address_1
        %td= contract.home_address_2
        %td= contract.home_city
        %td= contract.home_state
        %td= contract.home_zip
        %td= contract.parking_type
        %td= contract.preferences
        %td= contract.number
        %td= contract.created_at.to_formatted_s(:short)

        %td= link_to 'View', contract
        %td= link_to 'Edit', edit_contract_path(contract)
        %td= link_to 'Delete', destroy_contract_path(contract), method: :delete
