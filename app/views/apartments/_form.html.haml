= form_for @apartment do |f|
	- if @apartment.errors.any?
		#error_explanation
			%h2= "#{pluralize(@apartment.errors.count, "error")} prohibited this apartment from being saved:"
			%ul
				- @apartment.errors.full_messages.each do |msg|
					%li= msg

	.row
		= label_tag :image, 'Image'
		= cl_image_upload_tag('apartment[image_id]', crop: :limit, width: 600, height: 400, id: :uploader)
		.progress-container
			.progress
				.progress-bar#progress-bar{role: :'progressbar', style: 'width: 0%;'}
		.preview
	.row
		= f.label :title
		= f.text_field :title
	.row
		= f.label :description
		= f.text_area :description
	.actions
		= f.submit 'Save'
